# API URL (accessible from browser)
NEXT_PUBLIC_API_URL=http://localhost:3001

# API URL for server-side calls (may differ in Docker; defaults to NEXT_PUBLIC_API_URL)
# INTERNAL_API_URL=http://api:3001

# This app's URL (sent as Origin header for OriginGuard on server-side API calls)
WEB_URL=http://localhost:3000

NODE_ENV=development
